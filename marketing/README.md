# BiBoEmployeeTracking — marketing assets

## Site build (multi-language)

The landing page is generated into 7 locales (en + zh, ja, vi, id, fr, es).

- **Sources (edit these):** `src/template.html` (the page with `{{key}}` placeholders)
  and `src/i18n/<code>.json` (185 string keys per locale). `en.json` is the source of
  truth; keep all locales key-for-key in sync.
- **Generate:** `node build.mjs` → writes `site/index.html` (en, root) and
  `site/<code>/index.html` for each locale, plus `site/sitemap.xml`. It injects the
  per-locale `<html lang>`, canonical, `hreflang`/`x-default`, `og:locale`, JSON-LD url,
  and the nav language switcher.
- **Do NOT hand-edit `site/index.html` or `site/<code>/index.html`** — they are
  generated. Edit the template/JSON and re-run. Deploy rsyncs all of `site/` to `/`.

---

Brand graphics for the marketing site. All generated by [`gen.py`](gen.py)
(`python3 gen.py`) using the clock+person brand mark and the macOS system font (SF).
Re-run to regenerate after a brand tweak.

## Assets

### `logo/`
| File | Use |
|---|---|
| `mark-accent.png` (512) | Brand mark, blue, transparent — on light backgrounds |
| `mark-white.png` (512) | Brand mark, white, transparent — on dark/accent backgrounds |
| `app-icon.png` (512) | App icon (blue squircle + white mark) |
| `lockup-light.png` | Horizontal logo + “BiBoEmployeeTracking” (dark text) for light bg |
| `lockup-dark.png` | Same, white text, for dark bg |
| `favicon.png` (128) | Favicon source |

### `social/`
| File | Use |
|---|---|
| `og-image.png` (1200×630) | Open Graph / Twitter / LinkedIn share card |
| `square-1080.png` (1080×1080) | Instagram / square social posts |

### `features/` (160×160, soft-accent tiles)
`time` · `keyboard` · `screenshots` · `browser` · `private` · `menubar`
— one per core capability, for a features grid.

### `hero/`
| File | Use |
|---|---|
| `app-window.png` | **Real** screenshot of the app dashboard (rounded corners + soft shadow). Re-capture with `screencapture -R<x,y,w,h>` then round/shadow in PIL. |

### `screens/` — real product screenshots (rounded + shadowed)
`activity.png` · `screenshots.png` · `browser.png` · `permissions.png`
— actual app screens for a features/section gallery (currently dark theme).

## Brand

**Name:** BiBoEmployeeTracking · **Mark:** a person silhouette inside a clock ring
(employee + time). **Font:** San Francisco / system stack
(`-apple-system, BlinkMacSystemFont, "Segoe UI", system-ui, sans-serif`).

### Palette
| Token | Hex | Use |
|---|---|---|
| Accent | `#2D6CDF` | Primary brand blue |
| Accent dark | `#1A49A8` | Gradient end, hovers |
| Ink | `#1A1A1A` | Headings/body on light |
| Muted | `#6B6B6B` | Secondary text |
| Subtle | `#F6F6F6` | Cards / sections |
| Border | `#E3E3E3` | Hairlines |
| Green / Amber / Red | `#2FA85A` / `#E0A400` / `#D9453C` | Status: tracking / idle / paused |

## Suggested copy

- **Tagline:** Local-first time & activity tracking for macOS.
- **Sub:** See where work time goes — app usage, keystrokes, screenshots, and
  browser time — all stored on the machine, exported on your terms.
- **Feature blurbs:**
  - **App & window time** — idle-aware, so only active time counts.
  - **Keyboard activity** — counts only; the actual keys are never recorded.
  - **Periodic screenshots** — automatic, with retention limits.
  - **Browser pages & time** — via a lightweight extension.
  - **100% local** — no cloud, no account; export to CSV or JSON anytime.
  - **Menu bar** — start/stop and at-a-glance status (green / amber / red).

> Design note: flat, low-color, one accent — keep marketing visuals consistent with
> the app's quiet utilitarian style.
